<UserControl x:Class="Pasjans.View.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pasjans.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button Content="Tasuj karty" Command="{Binding ShuffleCommand}" Width="200" Height="40" Margin="5"/>
        <Button Content="Dobierz kartę" Command="{Binding DrawCardCommand}" Width="200" Height="40" Margin="5"/>
        <TextBlock Text="Tutaj będzie gra!" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <ListView ItemsSource="{Binding Deck}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,50,10,10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding ImageSource}"  />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
