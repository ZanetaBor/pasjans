<UserControl x:Class="Pasjans.View.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Pasjans.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:GameViewModel/>
    </UserControl.DataContext>
    
    <Grid Background="White">

        <!-- Sekcja górna -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Stos dobierania -->
            <RowDefinition Height="Auto"/>
            <!-- Miejsca na Asy -->
            <RowDefinition Height="*"/>
            <!-- Główna część gry -->
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Image Source="{Binding DrawPile.Count, Converter={StaticResource CardImageConverter}}" Width="100" Height="150" Margin="5"/>
            <Image Source="{Binding DiscardPile.Last.ImageSource}" Width="100" Height="150" Margin="5"/>
            <Button Content="Dobierz kartę" Command="{Binding DrawCardCommand}" Width="120" Height="40" Margin="10"/>
        </StackPanel>

        <!-- Miejsca na karty Asów -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <ItemsControl ItemsSource="{Binding FoundationPiles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="100" Height="150" BorderBrush="Black" BorderThickness="2">
                            <Image Source="{Binding ImageSource}" Width="100" Height="150"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- Główne stosy kart -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <ItemsControl ItemsSource="{Binding TableauPiles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <ItemsControl ItemsSource="{Binding}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding ImageSource}" Width="100" Height="150" Margin="2"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <Button Content="Tasuj karty" Command="{Binding ShuffleCommand}" Width="100" Height="40" Margin="20" VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>
