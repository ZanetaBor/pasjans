<UserControl x:Class="Pasjans.View.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Pasjans.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:GameViewModel/>
    </UserControl.DataContext>
    
    <Grid Background="White">
        <Button Content="Tasuj karty" Command="{Binding ShuffleCommand}" Width="60" Height="40" Margin="20" VerticalAlignment="Bottom" ContentStringFormat=""/>
        <Button Content="Dobierz kartę" Command="{Binding DrawCardCommand}" Width="60" Height="40" Margin="20" HorizontalAlignment="Right"/>
        <TextBlock Text="Tutaj będzie gra!" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Top"/>
        <ListView ItemsSource="{Binding Deck}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" Height="500" Margin="50">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding ImageSource}"  Width="100" Height="150" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
